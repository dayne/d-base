---
driver:
  name: vagrant

verifier:
  name: inspec

provisioner:
  name: chef_zero

platforms:
  - name: ubuntu-16.04
#  - name: centos-7.1

suites:
  - name: gibson
    run_list:
      - recipe[d-base::default]
      - recipe[d-base::gibson]
    attributes:
